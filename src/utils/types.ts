export interface SkillData {
  name: string;
  proficiency: number;
}

export interface ProjectData {
  id: number;
  title: string;
  description: string;
  image: string;
  techStack: string[];
  githubLink: string;
  liveLink: string;
}